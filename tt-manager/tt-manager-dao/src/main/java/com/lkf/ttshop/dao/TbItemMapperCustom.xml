<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lkf.ttshop.dao.TbItemMapperCustom" >
 <!-- <sql id = "Base_Column_List">
    id,title,sell_point,price,num,barcode,image,cid,status,created,updated
  </sql>
  <select id="listItemsByPage" resultType="com.lkf.ttshop.pojo.po.TbItem" >

    select
    <include refid="Base_Column_List" />
    from tb_item
    limit #{offset},#{rows}
  </select>
  <select id="countItems" resultType = "long">
    select count(*) as total from tb_item
  </select>-->

  <sql id="Base_Column_List" >
    i.id, i.title, i.sell_point as sellPoint,
    i.price, i.num, i.barcode, i.image, i.cid,
    i.status, i.created, i.updated,c.name as catName
  </sql>

  <select id="listItemsByPage" resultType="com.lkf.ttshop.pojo.vo.TbItemCustom" >
    select
    <include refid="Base_Column_List" />
    from tb_item i
    left join tb_item_cat c on i.cid=c.id
    limit #{offset},#{rows}
  </select>
</mapper>